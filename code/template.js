const inputReader = require('./input_reader.js')
//Moved input reader into reusable module that returns an array of the data

async function task() {

    const puzzleInput = await inputReader.processInput('');


}

task();